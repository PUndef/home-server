__pycache__/
*.pyc

backups/

# Local/throwaway scripts under scripts/openwrt/ (diagnostics, one-off probes).
# Convention: prefix with underscore to keep them out of the repo.
scripts/openwrt/_*

# Secrets and local env (never commit tokens or local overrides here).
*.env.local

# Python venvs (local tooling)
scripts/phoneserver/.venv-kuma/

# Abandoned duplicate / generated frontend artifacts
wf-farm-helper/
**/public/drops.html
