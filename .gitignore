__pycache__/
*.pyc

backups/

# Local/throwaway scripts under scripts/openwrt/ (diagnostics, one-off probes).
# Convention: prefix with underscore to keep them out of the repo.
scripts/openwrt/_*

# Secrets and local env (never commit per-system Beszel TOKENs here).
*.env.local
scripts/proxmox/morphic.env.local

# Cursor agent session artifacts.
agent-tools/
