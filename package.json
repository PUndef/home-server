{
  "name": "kb-remote-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Local dashboard for kb-remote: shows attached Mac→WSL folders, mutagen state, autossh tunnel, dev tmux sessions, and exposes restart/start/stop actions.",
  "type": "module",
  "bin": {
    "kb-remote-ui": "./bin/kb-remote-ui"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
